{
	"name": "kjroelke/wordpress-theme",
	"description": "a custom WordPress theme",
	"type": "project",
	"repositories": [
		{
			"type": "composer",
			"url": "https://wpackagist.org",
			"only": [
				"wpackagist-plugin/*",
				"wpackagist-theme/*"
			]
		}
	],
	"scripts": {
		"phpcs": "phpcs functions.php patterns/ inc/ --extensions=php --ignore=./build/",
		"phpcbf": "phpcbf functions.php patterns/ inc/ --extensions=php --ignore=./build/"
	},
	"config": {
		"allow-plugins": {
			"composer/installers": true,
			"dealerdirect/phpcodesniffer-composer-installer": true
		}
	},
	"require-dev": {
		"wp-coding-standards/wpcs": "*"
	}
}
